## B2B SaaS Dashboard

- Stack: Vite, React, TypeScript, CSS Modules.
- Feature-based architecture: This way we can control each feature and update them, so the whole concept relies on each feature.

### TODO list

- /_ TODO imporant refactor global vars as needed _/
- finish routing + at least 2 pages.
- fix expandUsers to accept the data when dataSource switches // const newList = list.length > 0 ? ApiUtils.expandUsers(list) : [];
- //TODO reorganize all types,
